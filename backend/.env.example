# Copy to .env and adjust. Never commit real secrets.

# HTTP port the server listens on.
PORT=8080

# Secret used to sign session JWTs. Set a long random value in production.
JWT_SECRET=dev-only-insecure-secret-change-me

# Session token lifetime (Go duration, e.g. 24h, 30m).
TOKEN_TTL=24h

# Seed demo users/listings on startup (true/false).
SEED_DEMO_DATA=true

# ---- CometChat (Phase B — not used yet) ----
# Documented here so the deployment surface is ready. Leave blank in Phase A.
COMETCHAT_APP_ID=
COMETCHAT_REGION=
COMETCHAT_API_KEY=
