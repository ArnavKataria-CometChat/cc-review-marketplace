// Top-level build file. Plugin versions are declared here with `apply false`
// and applied in the module build file, keeping the Android/Kotlin toolchain
// aligned across modules. This matches the proven local toolchain.
plugins {
    id("com.android.application") version "8.11.1" apply false
    id("org.jetbrains.kotlin.android") version "2.2.0" apply false
}
