<?xml version="1.0" encoding="utf-8"?>
<!--
  The dev backend runs over plain HTTP on the emulator host loopback (10.0.2.2).
  The CometChat SDK merges a networkSecurityConfig that disables cleartext, which
  would break API calls to our own backend even with usesCleartextTraffic="true"
  set on <application>. Re-permit cleartext, but ONLY for the local dev hosts, so
  everything else (including CometChat's own HTTPS endpoints) stays TLS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
